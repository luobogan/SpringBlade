server:
  port: 81

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
    loadbalancer:
      retry:
        enabled: true


