# 工作流：把 `wf_*` 能力下沉到 Flowable 引擎的迁移方案

> 目标：与其双写，不如将 `wf_*` 中**引擎尚未具备**的功能改造后集成进 Flowable，
> 避免重复维护、统一执行路径、提升一致性与可维护性。
> 适用范围：`blade-workflow`（SpringBlade + Flowable **7.1.0**）
> 关联：`工作流双写漂移治理.md`（双写边界 / P0 根因 / 方案 A 收口）
> 关联：`工作流双写漂移治理-方案C-事件驱动落地设计.md`（状态派生的事件驱动设计）

---

## 0. 现状核实（决定迁移边界的两个硬事实）

| 核实项 | 结果 | 影响 |
|---|---|---|
| `FlowableConfig` 是否设置 history level | **未显式设置**（走 Flowable 默认）；`IProcessService` 注释明确「无需 `history=full`」 | `ACT_HI_COMMENT` 需 ≥ **activity**（应满足）；`ACT_HI_DETAIL` 需 **full**（未开）→ 表单快照不可迁 |
| 是否使用 `addComment` / `addUserIdentityLink` | **全模块零处使用** | `ACT_HI_COMMENT` 与自定义 identity link 均为「未启用」状态，迁移即新增依赖，需实测验证 |
| `asyncExecutorActivate` | `false` | 异步路径暂不生效；若开启须满足方案 C §3.4 三项兜底 |
| `ACT_*` 与 `wf_*` 是否同 DataSource | 是（治理文档 §4） | 原子性的唯一来源，迁移后仍需保持 |

---

## 1. 迁移范围

按「引擎有无原生对应物」分三档。**迁错了比不迁更糟** —— C 档明确不动。

### 1.1 A 档：有原生对应物 —— 建议迁移

| `wf_*` 功能 | 迁入目标 | 引擎承载方式 |
|---|---|---|
| 审批意见 / 流转轨迹 `wf_approval_log` | `ACT_HI_COMMENT` | `taskService.addComment(taskId, procInstId, message)`，原生 comment 表 |
| 抄送(8) / 传阅(11) | 自定义 identity link | `addUserIdentityLink(taskId, userId, "cc"/"circulate")` —— Flowable `TYPE_` 接受任意字符串 |
| 实例运行状态（运行 / 结束 / 暂停） | `ACT_RU_EXECUTION` + `SUSPENSION_STATE_` + `ACT_HI_PROCINST` | 即治理文档 6a/6b/6c 的对齐对象，方案 C 已覆盖 |
| 任务待办 / 已办状态 | `ACT_RU_TASK`（活跃）/ `ACT_HI_TASKINST.END_TIME_` | 原生 |
| 会签 / 或签 / 依次 | BPMN multi-instance | 或签 = `completionCondition` `${nrOfCompletedInstances==1}`；依次 = `isSequential="true"` |

### 1.2 B 档：语义必须先钉死，否则不迁

**协办(7)** —— 唯一悬而未决项：

| 语义判定 | 结论 |
|---|---|
| 「协办人也能办结并推进流程」 | 可迁为 **candidate user** |
| 「仅协助、不推进，由主办人办结」 | 引擎无对应，**留 `wf_*`** |

现有证据倾向前者：`WfWriteHelper#terminate` 把协办与待办一并关闭
（`.in(WfTask::getStatus, WfTask.STATUS_TODO, WfTask.STATUS_COADJUTANT)`），
说明协办是「可被终结的待办态」。但 —— **「协办办结是否推进流程」必须业务确认**后才能定档。

### 1.3 C 档：不建议迁移（迁了更糟）

| 功能 | 为什么不迁 |
|---|---|
| **组织 / 数据权限** | 要迁就得把 org 同步进 `ACT_ID_USER` / `ACT_ID_GROUP` / `ACT_ID_MEMBERSHIP`，等于制造**第二份组织真相源**，与 blade-system 认证体系双写 —— 用更大的双写换掉小双写 |
| **表单快照 `wf_form_snapshot`** | 引擎侧 `ACT_HI_DETAIL` 需 **history=full**（当前未开）；且是扁平 KV 而非按节点 JSON，查「某节点快照」要扫 detail 表 —— **负优化** |
| **草稿(5)** | 引擎未发起，无对应物；本就是纯台账单写，不构成双写 |
| **催办 / 已读** | 纯 OA 通知态，引擎无概念 |
| **`wf_process_definition` / `node` / `link` / `operator`** | 这是**设计态**元数据，BPMN 是其产物（画布生成）。会签/或签可下沉 BPMN（已列 A 档），但操作者配置要**运行时解析**（`WfOperatorResolver`），BPMN 只能存配置、解析仍需自定义监听 —— 省不掉代码，反而把配置藏进 XML 更难维护 |

---

## 2. 改造目标

1. **统一执行路径** —— A 档各项只写引擎一次，`wf_*` 不再镜像同一事实，根除状态类漂移（P0 的根）。
2. **收口器瘦身** —— 迁完后 `WfWriteHelper` 里「状态镜像」部分可删，只剩 OA 独占写。
3. **减表** —— `wf_approval_log` 可退役（轨迹迁 comment）；`wf_task` 的抄送 / 传阅行可退役（迁 identity link）。
4. **明确保留项** —— B / C 档留在 `wf_*`，**不为「统一」而硬塞**。

### 2.1 迁移后 `wf_*` 残留清单（目标态）

| 表 | 保留内容 |
|---|---|
| `wf_instance` | 标题、业务关联（`form_id` / `data_id`）、`biz_key`、紧急度、草稿态、测试态、展示用节点名 |
| `wf_task` | 协办（依 B 档判定）、催办 / 已读、OA 展示字段 |
| `wf_form_snapshot` | 全保留（C 档） |
| `wf_process_definition` / `node` / `link` / `operator` | 全保留（设计态，C 档） |
| `wf_approval_log` | A 档迁移后可退役；若 `log_type` 分类仍需 OA 侧区分，则保留轻量版 |

> **目标不是消灭 `wf_*`，而是消灭重复维护。**

---

## 3. 约束条件（硬约束，违反即不可行）

1. **Flowable 表不可加列**
   只能用原生表 + 原生字段 + 变量表。不能给 `ACT_HI_COMMENT` 加 `node_key` 列 ——
   升级即丢 / 即崩，且废掉「可换引擎」演进位（治理文档 §5.4）。

2. **history level 门槛**
   `ACT_HI_COMMENT` 需 ≥ **activity**；`ACT_HI_DETAIL` 需 **full**。
   **开工前必须实测确认引擎实际 history level** —— 这是 A 档能否落地的第一道闸。

3. **禁止隐式约定**
   若某功能只能靠「把 `node_key` 塞进 `ACTION_` 字段」实现，等于把耦合从表结构搬到字符串约定，
   可读性下降。**此类宁可留在 `wf_*`**。

4. **查询不能退化**
   OA 高频查询（我的待办含协办 / 抄送、部门范围、多类型聚合）必须仍在关系层高效完成。
   任何「迁后要扫变量表或扫 FULL history」的方案都是负优化。

5. **同库事务前提不变**
   `ACT_*` 与 `wf_*` 同 DataSource，这是原子性唯一来源（治理文档 §4）。分库则一切免谈。

6. **版本锁定 7.1.0**
   升级 Flowable 必须回归 identity link type 与 comment 语义。

7. **存量数据迁移**
   历史 `wf_approval_log`、抄送 / 传阅行需一次性迁移，或双轨只读保留。

8. **可灰度、可回滚**
   A 档每项独立 `@ConditionalOnProperty` 开关，逐个迁移，**禁止一次性切换**。

9. **异步路径同样受方案 C §3.4 约束**
   若开 `asyncExecutor`，comment / identity link 的写入也需「禁 `HISTORIC_*` 事件
   + 常驻对账修复 + deadletter 告警」三项兜底。

---

## 4. 推进顺序

| 序 | 事项 | 理由与前置 |
|---|---|---|
| **0** | 实测确认 history level | 硬门槛，决定 A 档第 1 项能否落地 |
| **1** | 审批轨迹 → `ACT_HI_COMMENT` | 收益大、风险最小（只读展示型数据，不影响流转） |
| **2** | 抄送 / 传阅 → identity link | 需先确认「抄送不产生可办任务」 |
| **3** | 实例 / 任务状态 | 即方案 C，设计已就绪 |
| **4** | 会签 / 或签 / 依次下沉 BPMN | 改动面最大，需全量流程回归，**最后做** |
| **5** | 协办（B 档） | 待业务确认语义后再定档 |

---

## 5. 与既有方案的关系

```
方案 A（已落地）  WfWriteHelper 双写收口        → 防「漏写」复发
方案 C（设计就绪）引擎事件派生台账状态          → 消灭「状态镜像」双写
本方案（待启动）  把 OA 能力下沉进引擎原生能力  → 消灭「功能重复实现」
```

三者递进而非互斥：本方案第 3 步（实例 / 任务状态）即方案 C；
第 1、2 步完成后，`wf_approval_log` 与抄送 / 传阅行随之退役，`wf_*` 维护面进一步收窄。

---

## 6. 实施进度（截至 2026-09-26）

> 记录实际落地情况，便于后续接力。所有开关默认 **false**，灰度开启、一键回退。

### 6.1 阶段 0（history level 实测）— 已完成
- `FlowableConfig` 现已**显式** `setHistory("audit")` + `setAsyncHistoryEnabled(false)`（原为默认、存在歧义）。
- 结论：`ACT_HI_COMMENT` 在 `audit` 级可用（A 档第 1 项门槛满足）；`ACT_HI_DETAIL` 仍需 `full`（未开）→ **表单快照仍不可迁**（C 档，符合 §1.3）。
- ⚠️ 版本说明：本工程实际解析到的 Flowable 为 **8.1.0-SNAPSHOT**（根 pom 指向本地源码构建 `flowable-engine`），文档前文写的 7.1.0 已过时；升级/回归时按实际版本校验 identity link type 与 comment 语义。

### 6.2 A 档第 1、2 项（审批轨迹 / 抄送）— 双写接线已完成，读侧待运行时回归
按迁移策略「先双写校验、后切单写」，已做**写入侧开关隔离双写**（可逆、低风险），读侧暂保持 `wf_*` 权威：

| 项 | 写入中心 | 开关（默认关） | 行为 |
|---|---|---|---|
| 审批轨迹 → `ACT_HI_COMMENT` | `WfWriteHelper#appendLog`（全模块唯一写入口） | `blade.workflow.approval-comment.enabled` | 开关开时，按 wf 主键解析引擎 id 后 `addComment`；失败仅记日志不阻断 |
| 抄送/传阅 → identity link | `WfTaskServiceImpl#circulate` | `blade.workflow.cc-identity-link.enabled` | 开关开时，复用同一 `engineTaskId` `addUserIdentityLink(..., "circulate")`；不产生可办任务 |

**遗留（需运行时回归，非盲改）**：
1. 读侧切库：`wf_approval_log` 约 28+ 处读取仍走台账；切到 `HistoryService.createCommentQuery` 需全量回归。
2. 退役 `wf_approval_log` / 抄送 `wf_task` 行：须等读侧切换完成、存量数据双轨或一次性迁移（约束 §7）后再摘。
3. identity link 在任务完成后由 Flowable 清理（`ACT_RU_IDENTITYLINK`），历史抄送仍以 `wf_task` 为权威 —— 故即便开开关，也只做引擎侧关联预热，不删 `wf_task` 抄送行。

### 6.5 阶段 1 会签多实例「运行时门禁」已接线（2026-09-26）
`WfDefinitionServiceImpl` 部署期注入多实例（原已存在，开关 `blade.workflow.engine-multi-instance.enabled` 默认关）的**运行期前置条件已全部打通**，开关即具备「真正切到引擎多实例」的能力，无需改业务代码：

| 改动点 | 内容 | 受开关保护 |
|---|---|---|
| `WfDefinitionServiceImpl#applyMultiInstanceIfEnabled` | 注入多实例时把 userTask.assignee 接成 `${wfMiAssignee}` 并清空候选用户，保证 MI 任务按人拆分 | 是 |
| `WfTaskServiceImpl#doApprove` | 引擎多实例节点跳过自研会签计数（countPending/closeSiblings/激活下批），只 `completeTask` 当前引擎任务 | 是（isEngineMultiInstance） |
| `WfInstanceServiceImpl#advance` | MI 节点按「每条引擎任务=一人」生成 wf_task（避免 N×N），并对被完成条件取消的兄弟待办做对账收口 | 是 |
| 集合变量供给 | 每次流转（startInstance/completeTask/moveActivity）前把 `wfMiAssignees_<nodeKey>` 注入流程变量，进节点前即就绪、幂等持久 | 是 |

**开关仍默认 false**（避免未回归即上线导致「节点进入时缺集合变量运行期失败」）。开启前的回归验证清单：
1. 或签：首办即过 → 引擎取消其余 MI 任务 → 对账收口其 wf_task（不残留待办）；
2. 会签：全部办完才推进；任一人办完不推进、wf_task 不丢；
3. 依次：逐人激活，最后一人办完才推进；
4. 退回进入多实例节点：集合变量就绪、任务按人展开；
5. 单审批人普通节点（signOrder=0 但仅 1 人）：MI 退化为单任务，行为不变；
6. 协办/征询：按节点去重，不随 MI 多引擎任务重复生成。

> ⚠️ 边界：首节点即多实例且操作者依赖实例上下文（instId）的场景，发起阶段 instId 尚为空、按静态节点配置解析；动态（依赖表单/实例）首节点多实例为边界情况，须专项回归。

#### 6.5.1 验证手段（已落地）
- **引擎侧（可立即跑，H2 内存，不依赖 Nacos/MySQL）**：`src/test/java/org/springblade/workflow/WfMultiInstanceGateTest.java`。
  部署与 `applyMultiInstanceIfEnabled` **同形态**的多实例 BPMN（assignee=`${wfMiAssignee}` + 三种完成条件），
  用真实 Flowable7 引擎断言：会签全办才推进、或签首办取消兄弟、依次逐人激活。
  运行：`mvn -pl blade-service/blade-workflow test -Dtest=WfMultiInstanceGateTest`（需能解析 `flowable-engine:8.1.0-SNAPSHOT` 的仓库）。
- **wf_task 侧（依赖 wf_* 表，MySQL）**：已落地 `WfTestServiceImpl#miCounterSignScenario(formId)` + `WfTestController#POST /test/mi-scenario`。
  它**程序化造**三条「单 MI 节点」定义（nodeType=1 + signOrder=1/0/2 = 会签/或签/依次），经与正式部署同口径的
  `deployForTest`（开关开启时注入多实例）真实发起测试实例，用 `IWfTaskService#autoApprove(system=true)` 逐人驱动门禁，
  断言 `wf_task`：进入时引擎任务数（并行=3/串行=1）、或签首办即过且兄弟待办被对账关闭（pending→0）、会签 pending 序列 [2,1,0]、依次 [1,1,0]，末办才结束。
  运行：开启 `blade.workflow.engine-multi-instance.enabled=true` 后，`POST /api/blade-workflow/test/mi-scenario?formId=<已有表单ID>`；返回 `allPassed` 与逐模式明细。
  注：本方法自带定义/实例/部署自清理（finally），但仍建议仅在测试库执行。

### 6.3 C 档（保留范围）— 已固化，不迁移
以 §1.3 为准，**明确不动**：组织/数据权限、表单快照 `wf_form_snapshot`、草稿(5)、催办/已读、`wf_process_definition/node/link/operator` 设计态元数据。任何「为统一而硬塞」均违反 §2.4 与约束 §3。

### 6.4 新增开关清单（汇总）
| 开关 | 默认 | 作用 |
|---|---|---|
| `blade.workflow.ledger-listener.enabled` | false | 方案C 全局事件监听（影子模式，仅记日志不写库） |
| `blade.workflow.approval-comment.enabled` | false | 审批轨迹双写 `ACT_HI_COMMENT` |
| `blade.workflow.cc-identity-link.enabled` | false | 抄送/传阅双写 identity link |
